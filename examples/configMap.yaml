apiVersion: v1
kind: ConfigMap
metadata:
  name: goma-config
data:
  goma.yml: |
    # Goma Gateway Configuration File
    version: 2
    gateway:
      # Timeouts in seconds
      timeouts:
        write: 30
        read: 30
        idle: 30
